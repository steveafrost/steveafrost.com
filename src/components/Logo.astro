---
/**
 * Faceted crystal logo — geometric gem with internal facet lines.
 * The coral-to-amber gradient shifts across faces to create depth.
 */
---

<svg
  class="logo-mark"
  viewBox="0 0 36 36"
  fill="none"
  xmlns="http://www.w3.org/2000/svg"
  aria-hidden="true"
>
  <defs>
    <linearGradient id="logo-grad" x1="4" y1="2" x2="32" y2="34" gradientUnits="userSpaceOnUse">
      <stop offset="0%" stop-color="#ED6A5E" />
      <stop offset="50%" stop-color="#E8836B" />
      <stop offset="100%" stop-color="#F4A261" />
    </linearGradient>
  </defs>

  <!-- Outer crystal silhouette -->
  <path
    d="M18 2 L30 10 L30 26 L18 34 L6 26 L6 10 Z"
    fill="url(#logo-grad)"
    opacity="0.2"
  />

  <!-- Top-left face -->
  <path d="M18 2 L6 10 L18 18 Z" fill="url(#logo-grad)" opacity="0.55" />

  <!-- Top-right face -->
  <path d="M18 2 L30 10 L18 18 Z" fill="url(#logo-grad)" opacity="0.75" />

  <!-- Right face -->
  <path d="M30 10 L30 26 L18 18 Z" fill="url(#logo-grad)" opacity="0.5" />

  <!-- Bottom-right face -->
  <path d="M30 26 L18 34 L18 18 Z" fill="url(#logo-grad)" opacity="0.35" />

  <!-- Bottom-left face -->
  <path d="M18 34 L6 26 L18 18 Z" fill="url(#logo-grad)" opacity="0.6" />

  <!-- Left face -->
  <path d="M6 26 L6 10 L18 18 Z" fill="url(#logo-grad)" opacity="0.45" />

  <!-- Facet edge lines -->
  <path
    d="M18 2 L18 18 M6 10 L18 18 M30 10 L18 18 M30 26 L18 18 M18 34 L18 18 M6 26 L18 18"
    stroke="url(#logo-grad)"
    stroke-width="0.6"
    opacity="0.4"
  />

  <!-- Outer edge highlight -->
  <path
    d="M18 2 L30 10 L30 26 L18 34 L6 26 L6 10 Z"
    stroke="url(#logo-grad)"
    stroke-width="0.8"
    opacity="0.6"
    fill="none"
  />
</svg>

<style>
  .logo-mark {
    width: 2rem;
    height: 2rem;
    transition: transform 0.3s ease, filter 0.3s ease;
    filter: drop-shadow(0 0 6px rgba(237, 106, 94, 0.15));
  }

  :global(.group:hover) .logo-mark {
    transform: scale(1.12) rotate(-4deg);
    filter: drop-shadow(0 0 10px rgba(237, 106, 94, 0.35));
  }

  @media (prefers-reduced-motion: reduce) {
    .logo-mark {
      transition: none;
    }
    :global(.group:hover) .logo-mark {
      transform: none;
      filter: drop-shadow(0 0 6px rgba(237, 106, 94, 0.15));
    }
  }
</style>
